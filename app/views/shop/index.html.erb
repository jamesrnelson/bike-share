<% @item.each do |item| %>
  <% if item.status == 1 %>
    <ul>
      <li><%= item.image %></li>
      <li><%= item.name %></li>
      <li><%= item.price %></li>
      <%= button_to "Add to Cart", cart_path(item_id: item.id) %>
    </ul>
  <% end %>
<% end %>
